# Specify Ruby version (optional but good practice)
ruby "~> 3.2.0"

# Source for gems
source "https://rubygems.org"

# Rails framework
gem "rails", "~> 8.0.0"

# PostgreSQL adapter
gem "pg", "~> 1.5"

# Puma web server (default in Rails)
gem "puma", "~> 6.0"

# Required by rails
gem 'bootsnap', '>= 1.18', require: false

# Redis Cache
gem "redis"

# Sidekiq
gem 'sidekiq', '~> 8.0'
gem 'sidekiq-cron', '~> 2.3'

# JSON
gem 'json', '~> 2.15'